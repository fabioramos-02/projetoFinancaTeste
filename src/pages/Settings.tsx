import React from 'react';

export const Settings: React.FC = () => {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-gray-900">Settings</h1>
      {/* We'll implement the settings page in the next step */}
    </div>
  );
};